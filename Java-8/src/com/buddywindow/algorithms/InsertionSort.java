package com.buddywindow.algorithms;

public class InsertionSort {
	
	public static void main(String ar[]) {
		int intArray[] = {32,7,11,-11,2,22,-15,67,3};
		//Here we set unsortedIndex is 1 because we assume the first element is in sorted order
		for(int unsortedIndex = 1 ; unsortedIndex < intArray.length ; unsortedIndex++) {
			//we need to store this element because we need to overwrite it by other element
			int newElement = intArray[unsortedIndex];
			for(int i = unsortedIndex ; i>0)
			
			
		}
		
		
		
		for(int i = 0; i<intArray.length - 1; i++) {
			int element = intArray[i+1];
			for(int j = i; j<=j; j--) {
				if(intArray[j] )
			}
			
		}
	}
}
